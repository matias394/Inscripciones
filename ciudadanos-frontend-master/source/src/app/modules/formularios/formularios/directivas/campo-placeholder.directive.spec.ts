import { CampoPlaceholderDirective } from './campo-placeholder.directive';

describe('CampoPlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new CampoPlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
