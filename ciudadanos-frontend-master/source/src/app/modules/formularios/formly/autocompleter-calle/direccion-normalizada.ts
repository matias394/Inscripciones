export class DireccionNormalizada {
    nombre: string;
    x:number;
    y:number;

    constructor(calleAltura:string, x: number, y:number) {
        this.nombre = calleAltura;
        this.x = x;
        this.y = y;
    }
}