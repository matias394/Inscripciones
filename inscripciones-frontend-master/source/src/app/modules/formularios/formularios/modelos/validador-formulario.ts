/**
 * Al agregar validadores tiparlos dentro de la propiedad validador
 * de la siguiente forma: 'nombreBeanValidador1' | 'nombreBeanValidador2' | ...
 */
export interface ValidadorFormulario {
  validador: 'model.validador.renaper';
  claveCampo: string;
}
